# دليل التثبيت والتشغيل - تاكسي برو

## المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:
- **Node.js** (الإصدار 18 أو أحدث)
- **npm** أو **yarn**

## خطوات التثبيت

### 1. تثبيت التبعيات

```bash
npm install
```

أو باستخدام yarn:

```bash
yarn install
```

### 2. إعداد ملف البيئة

انسخ ملف `.env.example` إلى `.env.local`:

```bash
cp .env.example .env.local
```

### 3. تشغيل المشروع في وضع التطوير

```bash
npm run dev
```

أو:

```bash
yarn dev
```

سيعمل المشروع على: [http://localhost:3000](http://localhost:3000)

## الوصول للوحة التحكم

### تسجيل الدخول للمدير

1. افتح المتصفح واذهب إلى: [http://localhost:3000/admin/login](http://localhost:3000/admin/login)
2. استخدم بيانات الدخول التجريبية:
   - **اسم المستخدم**: `admin`
   - **كلمة المرور**: `admin123`

### الصفحات المتاحة

- **الصفحة الرئيسية**: `/`
- **تسجيل دخول المدير**: `/admin/login`
- **لوحة التحكم**: `/admin/dashboard`
- **إدارة السائقين**: `/admin/drivers`
- **خريطة السائقين**: `/admin/map`
- **إدارة الطلبات**: `/admin/orders`
- **الإعدادات**: `/admin/settings`

## بناء المشروع للإنتاج

```bash
npm run build
```

ثم تشغيله:

```bash
npm start
```

## المميزات الرئيسية

### ✅ صفحة رئيسية جذابة
- تصميم عصري باللغة العربية
- دعم كامل لـ RTL
- واجهة سهلة الاستخدام

### ✅ لوحة تحكم المدير
- إحصائيات شاملة وتفصيلية
- رسوم بيانية تفاعلية
- تقارير العمل اليومية والشهرية

### ✅ إدارة السائقين
- إضافة سائق جديد
- تعديل بيانات السائق
- حذف سائق
- تغيير حالة نشاط السائق
- عرض جميع البيانات (اسم، موبايل، نوع السيارة، موديل، رقم اللوحة)

### ✅ خريطة تتبع السائقين
- عرض مواقع السائقين على الخريطة
- تحديث مباشر للمواقع (كل 5 ثواني)
- إمكانية اختيار سائق محدد لعرضه
- تصفية السائقين النشطين/غير النشطين

### ✅ إدارة الطلبات
- إضافة طلب جديد
- اختيار السائق
- تحديد نقطة الانطلاق والوجهة
- تحديد تكلفة الطلب
- حساب العمولة تلقائياً
- تغيير حالة الطلب (معلق، قيد التنفيذ، مكتمل، ملغي)
- تقرير أرباح السائق
- حساب إجمالي الطلبات والعمولات لكل سائق

### ✅ إعدادات الشركة
- تغيير نسبة العمولة
- تخصيص اسم الشركة
- تغيير العملة
- معاينة أمثلة على حساب العمولة

## التقنيات المستخدمة

- **Next.js 14** - React Framework
- **TypeScript** - للتحقق من الأنواع
- **Redux Toolkit** - إدارة الحالة
- **Material-UI (MUI)** - مكتبة واجهة المستخدم
- **RTL Support** - دعم كامل للغة العربية
- **Axios** - للاتصال بالـ API
- **Leaflet** - خرائط تفاعلية
- **Recharts** - رسوم بيانية

## ملاحظات مهمة

1. **البيانات التجريبية**: المشروع يحتوي على بيانات تجريبية للسائقين والطلبات
2. **التتبع المباشر**: محاكاة تحديث مواقع السائقين كل 5 ثواني
3. **الخرائط**: تستخدم OpenStreetMap المجانية
4. **API**: حالياً البيانات محلية في Redux، يمكن ربطها بـ API خلفي لاحقاً

## التطوير المستقبلي

- ربط المشروع بـ API خلفي حقيقي
- إضافة نظام إشعارات
- تطبيق موبايل للسائقين والعملاء
- تكامل مع خدمات الدفع
- إضافة تقارير متقدمة
- نظام دردشة بين المدير والسائقين

## الدعم

للمساعدة أو الاستفسارات، يرجى التواصل عبر:
- البريد الإلكتروني: info@taxipro.sa
- الهاتف: +966 50 123 4567

